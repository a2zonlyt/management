<p id="notice"><%= notice %></p>

<p>
  <b>Subject:</b>
  <%= @course.subject %>
</p>

<p>
  <%= image_tag @student.attachments.first.photo.url(:medium) if @student.attachments.present? %>
</p>


<%= link_to 'Edit', edit_course_path(@course) %> |
<%= link_to 'Back', courses_path %>